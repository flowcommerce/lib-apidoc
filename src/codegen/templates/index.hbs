{{#resources}}
import {{resourceClassName this}} from './{{resourceFileName this}}';
{{/resources}}


const enums = {
{{#enums}}
  {{toCamelCase name}}: {{{jsArrayStr values}}}
{{/enums}}
};


export default class {{className name}}Client {
  constructor(opts) {
    let options = opts;
    if (typeof opts === 'string') {
      options = { host: opts }; // convert host string to options object
    }

    this.enums = enums;

    {{#resources}}
    this.{{resourceInstanceName this}} = new {{resourceClassName this}}(options);
    {{/resources}}
  }
}
